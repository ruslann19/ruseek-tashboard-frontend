version: "3.8"

services:
  frontend:
    container_name: ruseek-tashboard-frontend
    build: .
    ports:
      - "9000:9000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
